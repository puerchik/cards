import { Button } from '@/components/ui/button'

export function App() {
  return (
    <div className="container">
      <Button disabled variant="primary">
        Button primary
      </Button>
    </div>
  )
}
