import { Button } from '@/components/ui/button'

export function App() {
  return (
    <div className="container">
      <Button as="a" icon variant="primary">
        Button primary
      </Button>
    </div>
  )
}
